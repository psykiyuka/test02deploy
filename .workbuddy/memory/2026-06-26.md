
## Home 首页分类导航调整（2026-06-26 22:00）

### 需求
首页分类导航原为：数码/服装/食品/礼品。要求去掉"礼品"，"服装"→"电器"，"食品"→"办公"，并更换对应图标。

### 修复
- 文件：`shop-frontend/src/pages/Home.vue`
- import：`Shirt, Cookie, Gift` → `Plug, Briefcase`
- categories 数组：4 项改为 3 项，即 `数码(Smartphone)` / `电器(Plug)` / `办公(Briefcase)`
- 图标均来自 lucide-vue-next，已确认 `Plug`/`Briefcase` 为有效导出。

## ProductList 已选分类标签显示真实分类名（2026-06-26 21:49）

### 需求
商品列表页左侧选中分类后，右侧搜索框下方的"已选条件"标签原显示固定文案"分类已选"，需改为显示真实分类名（如"智能手机"）。

### 修复
- 文件：`shop-frontend/src/pages/ProductList.vue`
- 新增计算属性 `selectedCategoryName`：遍历 `categories`（一级）及其 `children`（二级），匹配 `selectedCategory` 的 id 返回对应 `name`。
- 模板第 420 行（原第 408 行）：将固定文案 `分类已选` 替换为 `{{ selectedCategoryName }}`。
- `categories` 为树形结构（一级 `category.children` 含二级），`selectedCategory` 可能是任一级 id，故计算属性需两级查找。

## 找回密码测试本地跑通进度（2026-06-26，承接 06-25）
- 本地 Anaconda pydantic 1.10.12 与 fastapi 0.115.0 冲突 → 升级 pydantic 至 2.13.4（anaconda-cloud-auth 报冲突可忽略）。
- conftest.py 补 `DATABASE_URL=...@localhost:5432`、`JWT_SECRET`，新增 session 级 `init_pool()/close_pool()` fixture。
- user_service.send_reset_code 用 `asyncio.run()` 驱动 async 邮件发送，否则 mock 捕获列表为空。
- e2e 测试请求路径需加 `/api/shop` 前缀（main.py 全局 prefix）。
- JWT 撤销改为"时间戳"方案：`create_token` 加 `iat`(浮点)，撤销标记存重置时间戳(浮点)，`get_current_user` 仅拒 `iat < revoke_ts` 的旧 token，重置后新签发 token 不受影响。
- 结果：单测 14 + e2e 4 全通过；仅剩 `test_e2e_flow_and_token_revocation` 1 例仍因时序精度问题待确认（iat 浮点已改但 PyJWT 行为需复测）。
